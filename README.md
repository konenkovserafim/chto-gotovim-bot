# Что готовим? — SQLite v1

Переход на SQLite.

Что изменилось:
- рецепты импортируются из `recipes.json` в `bot_data.db`;
- избранное хранится в SQLite;
- список продуктов хранится в SQLite;
- холодильник хранится в SQLite;
- `user_data.json` больше не нужен.

Запуск на Railway как раньше: переменная `BOT_TOKEN` обязательна.
